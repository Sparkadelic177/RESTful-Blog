<% include ./partials/header %>
<div class="ui main text container segment ">
  <div class="ui huge header">New Blog</div>
  <form class="ui form" action="/blogs" method="post"> <!--this goes to the post request-->
      <div class="field">
        <label for="title"></label>
        <input type="text" name="blog[title]" placeholder="title"> <!--the name goes to the data base a request-->
      </div>
      <div class="field">
        <label for="image"></label>
        <input type="text" name="blog[image]" placeholder="image">
      </div>
      <div class="field">
        <label for="body"></label>
        <textarea name="blog[body]"></textarea>
      </div>
      <input class="ui green inverted button" type="submit">
  </form>
</div>
<% include ./partials/footer %>
